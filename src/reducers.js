const initialState = {
  access_token: '',
  flags: {
    login: false
  }
};

const linkApp = (state = initialState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default linkApp;
